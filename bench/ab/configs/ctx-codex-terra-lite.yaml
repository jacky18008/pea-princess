# Part of Pea Princess (vet-flat) by Hsien Hao (Jacky) Chen - https://github.com/jacky18008/pea-princess - CC BY 4.0
# Context-slimming experiment cell (2026-09-12): the same appendix for every arm; the arm is the skill snapshot.

name: ctx-codex-terra-lite
agent: codex
phase: ablation
factor: "context slimming: baseline vs arms A/B/C, codex lite"
description: |
  One cell of the context-slimming experiment (docs/handoffs/2026-09-11-evening.md, Context slimming).
main_model: gpt-5.6-terra
worker_model: null
budget_mode: lite
allowed_tools: null
append_system_prompt: |
  You are running on a limited plan. Follow .agents/skills/vet-flat/references/budget-modes.md
  `lite` exactly: at most 8 tool calls, run scripts only, in this order: `epc.py search --postcode` then `epc.py cert <id>` for the matching flat (never pass an address to `cert`), `geo.py lookup` then `crime.py box
  --months 3 --no-sensitivity`, `commute.py journey`, `company.py profile` when an entity is
  named), never read a file over 40 KB, ask the user once for anything else, and still deliver
  the verdict card, the hard-filter table and two killer questions from
  references/questions.md.
  This is an unattended benchmark run: there is no user to answer questions, so never stop to ask; when an input is unavailable mark that axis unknown (U) and still write report.json.
notes: |
  Baseline = tag baseline-context-2026-09-12. Codex sub-agent threads are not in the runner's token count; account from ~/.codex/sessions before quoting.
