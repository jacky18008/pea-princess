# V3: bounded prose consistency verification

V2 stops after two physical calls, with 414,497 known processed tokens. Its real scan completed and actual free-text input preserved the correct current conditions. The accepted second reply nevertheless described a retired direct-sun condition as a preference and ordinary daylight as an adopted bonus. Matching metadata did not catch those visible discrepancies. All six remaining V2 cells stay unrun. V1's failure and 306,247 tokens remain unchanged.

V3 extends the finite prose checker to explicit assertions of supported condition strengths, including reintroduction of retired fields. It must still allow analyst-owned advice, hypothetical choices, negations and historical descriptions. A short host reminder distinguishes retirement from weakening; this is not a universal language or source-truth guarantee. The coordinator also stops on a rejected/missing required host guard and supports an independently documented review stop without rewriting physical receipts.

Before dispatch, replay the retained V2 response through the new checker offline: require the known mismatch to be contained, retain exact original bytes and do not replace the historical response. Run positive/negative regressions and host integration checks. Freeze a new commit, public ZIP, runtime and private plan.

Plan **one new four-turn cached-evidence conversation**, Terra low effort, standard depth, agent output. Use the same previously frozen cached-history case and exact four user-turn policies from V2, with the original saved source attachments. This is a new versioned development regression, not a replacement V2 row or a new live scan. No research permission is added. The actual UI supplies free text in T2 and a self-contained daylight-as-bonus selection in T4, if offered. Restart the owned idle host before T4 to check continuity. Missing suitable controls stops the episode; do not fabricate an option.

The original task bound remains 1,200,000 known processed tokens. V1 plus V2 already used 720,744, leaving **479,256**, shared by all V3 calls. At most seven physical calls across versions will have run if V3 completes; no other matrix is resumed. Review first-call cost and conservatively forecast remaining turns before further dispatch. These are between-call stopping bounds, not provider hard caps. Unknown usage, timeout, integrity failure, a rejected reply or a failed independent authority/publication gate stops further calls. No automatic repair, retry, new repetition or hidden replacement.

The original rubric applies. Independently review the entire observed conversation, not merely metadata. Report the completed V2 live scan separately from V3 cached dialogue; they do not constitute a clean four-turn live episode on a single version. Successful containment also is not a successful conversational answer. Evaluator/development usage remains separate from actor receipts. Claude stays paused. This amendment does not authorize promotion to the main service solely from compilation or a matching checker result.
