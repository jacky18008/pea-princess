# V2: bounded follow-up after the retained V1 failure

V1 is stopped after one physical call, with **306,247 known processed tokens**; its other eleven cells remain unrun. The model completed and the host condition guard accepted it, but the actual scan failed while checkpointing an unresolved location (`how_located=None`). Independently, two native web-search events contained conflicting duplicate `item.id` values; the Inspector correctly marked tool identity ambiguous and the coordinator halted. The unique terminal usage is independently available. These outcomes and the original frozen runtime are retained, not rewritten as a pass.

V2 corrects the scanner's unresolved-location checkpoint and adds an end-to-end offline regression from a postcode/street input through the real scanner/checkpoint path with mocked sources. It does not weaken duplicate-key integrity checks or change the intent guard to accommodate the observed prose. The original rubric still applies.

To remain within the original task allowance, V2 plans only **two new four-turn conversations**: one fresh standard scan and one independent cached-evidence conversation. The cached case uses the same preselected historical source attachment. These are new versioned cases, not replacement rows in V1. At most nine physical calls across V1 and V2 are now planned for execution, below the original twelve-call boundary. No third new-version repetition or cross-model conclusion is claimed.

The original task token budget remains **1,200,000**; V1 spend is not forgiven. V2's remaining global boundary is **893,753** known processed tokens, with per-session ceilings of 550,000 live and 343,753 cached. All limits remain between-call stopping bounds and a single call may overshoot. The first V2 receipt is retained for a new cost checkpoint; use its cost plus 250,000 for live follow-ups and 300,000 for cached dialogue as the initial continuation forecast, revising conservatively from actual usage before later calls. Unknown usage, another integrity failure, timeout, guard rejection or unsuitable required controls stops the new coordinator. No model repair, automatic retries or hidden replacement calls.

Exact same user inputs as V1, except the internal T4 selection policy is made precise: select an actually offered self-contained **daylight-as-bonus, not an exclusion** option, with no unrelated permission. V1's internal policy used the broader word preference despite its rubric expecting bonus; V1 never reached T4. This clarification does not fabricate an option or alter user answers. No free-text fallback replaces a missing option.

Freeze a new public ZIP/runtime/plan before dispatch, register this amendment against the same task allowance, and keep both versions' evidence. A V2 success does not erase V1's failure. If native tool identity ambiguity recurs, report the trace limitation rather than silently taking the last JSON key or claiming all research was verified.
