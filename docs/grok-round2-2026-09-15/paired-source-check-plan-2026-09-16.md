# Preregistered two-repeat model check after source-integrity fixes

Recorded before either paired model call. The completed six-turn cohort is frozen and cannot inherit later fixes. The specific failure to retest is B's previously omitted TfL **rail 08:36 arrival** versus its all-mode **08:58 Underground endpoint**. Both must be shown alongside A's 08:36 rail arrival without treating B as a 09:00 near miss on the rail alternative.

Two independent, otherwise empty Grok CLI test projects were staged from the **same** published candidate ZIP SHA-256 `1263662eea308d36428f770df76aad9dcd95d08e8678a9465b1a2225124563a9`, installed `SKILL.md` SHA-256 `a59b36f5fbc826ff088b24d2d32a5f1a361a11fcd5777c94fa4b8dc4aceb58ab` and the **same** byte-identical frozen opening user text SHA-256 `125e7cd0140f610b7db2454f33e0329cd2ccdcc1e9085dcd3e87c5bbe2469208`. Hosts are Grok Build CLI 1.0.30, `grok-4.6-build`, high effort, `--no-subagents`, new native session/task-only OAuth home per repeat, soft observation after 14 model inner calls, hard stop after 32, no auto retry. Original version's opening 1,212,479 processed tokens is a historical point of comparison, not a paired randomized baseline. Distinct runs use `/private/tmp/pea-grok-patched-scan-01-20260916` and `-02-20260916`; stage manifests/raw output belong in the owner-only `pea-princess-evidence-20260915/grok-end-to-end-20260915` folder. Neither test has old actors, drafts or previous journals in its project root.

Record each complete `end_turn`/tool trace, official TfL source JSON statuses for **all**, **rail** and **bus**, arrival time, exact endpoint and origin/date/door buffer, invocation of `commute_compare.py` or an equivalent script, first visible sentence, advert-versus-EPC distinctions, whether B EPC/bills remain unknown, candidate/viewing authority and actual listing/contact tool calls. A model statement without a corresponding official source receipt or observed verifier call is unverified. A deadline, hard stop or provider limit creates a preserved partial record; do not silently retry or select only favorable runs.

Both complete runs must avoid the specific same-endpoint/source error and preserve user authority to count as a provisional **two-sample** improvement; one repeat failing makes the issue unresolved and calls for a third frozen independent repeat if usage allows. Passing two samples does **not** prove general model reliability, source authenticity, complete six-turn save/reopen acceptance or paid-subscription capacity. Evaluate conversational first lines independently, even when technical route checks pass. Keep both runs and their failures in the report.
