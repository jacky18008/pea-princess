# Preregistered complete patched-package six-turn acceptance

The historical six-turn cohort and its model outputs remain immutable. This is a **new complete journey** on one physically installed candidate public `pea-princess` archive SHA-256 `1263662eea308d36428f770df76aad9dcd95d08e8678a9465b1a2225124563a9` and installed `SKILL.md` SHA-256 `a59b36f5fbc826ff088b24d2d32a5f1a361a11fcd5777c94fa4b8dc4aceb58ab`. The opening comparison's two independent repeats are a separate experiment; no answers, research receipts or state from them enter this journey. This preregistration is committed before dispatch. The private owner-only stage manifest records each ZIP member/actor byte hash, CLI/model/home/project, and branch commit before any calls.

Use the exact six pinned `actor-01.txt` through `actor-06.txt` from the original owner-only frozen inputs, preserving their bytes. The first five turns continue the same new native Grok Build CLI 1.0.30 conversation, with `grok-4.6-build`, high effort, no subagents, soft observation at 14 inner model calls, hard stop at 32, 3,600-second deadline per turn and no silent retry. The user-supplied Foxtons extracts do not grant advertisement fetching, agent contact or viewing booking. If a turn stops incomplete, preserve the raw trace and classify all downstream steps unverified instead of inventing a continuation.

After the fifth turn, copy only the verified journal, current checkpoint, registered content-addressed documents, review/context and original captured evidence to an otherwise empty **different project**, physically install the same exact archive, and begin a new Grok conversation on exact actor-06. Neither old actors/drafts nor native conversation history may be present in the new project. Verify the state and source hashes **before reading its model answer**; after turn six verify candidate-specific holds, source/research completeness, saved TODO and current checkpoint. The earlier contaminated T6 is an audit failure that may not be used as the patched sample.

Require every terminal reply to have `end_turn`, matching frozen user/skill bytes and observed tools. Check advert rents, bedrooms and advertised areas, official TfL modes/endpoint/arrival-buffer facts (B rail 08:36 versus the distinct all-mode 08:58 Underground endpoint), A EPC address-versus-unit identity, unknown B unit/EPC/heating/bills and source failure/cached status. Check literal user authority: £2,200 rent-only global rule, mandatory quietness with unknown same-unit nighttime proof, A-only £2,450 exception for **comparison only**, both no-contact/no-viewing holds and B research continuing. T2/T5 current narrow comparison gates and the stricter full-fidelity gate require separately captured receipts and field-specific TODO; a model-written source note labeled `external` is not host-authenticated evidence. Run available deterministic checkers independently on the saved journal and model reply. Treat missing source/choice or uninvoked guard as unverified, never pass.

Grade the first visible sentence and final answer separately with independent Astra/high on directness, useful A/B comparison, unnecessary internal jargon/arithmetic and at most one helpful next choice. Native `ask_user_question` in headless CLI may refuse; only a literal label and real user selection on an **interactive Grok Bot** can satisfy the choice UX gate. Build CLI and Grok Bot are distinct hosts. A full pass needs every source/authority/save/reopen/action/UX gate, not only six answers. Capture per-turn model/tool counts, input/cache/output/reasoning, elapsed and API-equivalent cost ticks. Do not equate ticks with the subscriber's weekly quota; take an actual refreshed account percentage before/after if available, otherwise leave $20/$31 capacity unquantified. Publish all failures and raw transcripts even if any gate fails.
