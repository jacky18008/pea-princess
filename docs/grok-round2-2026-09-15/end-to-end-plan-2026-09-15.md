# Pinned end-to-end Grok acceptance: reply, changes, save, reopen

Prerecorded 2026-09-15 before any calls of this new cohort. Test a newly built **public `pea-princess` ZIP** from the isolated `codex/grok-end-to-end-acceptance-2026-09-15` merge of experimental v4 and `main` commit `2cad479`. Pin its archive SHA, installed `SKILL.md` SHA, all installed file SHA, Grok Build CLI version, default effort, mode/depth, OAuth home, output and source status in the private receipt before the first request. The old tested v4 17-call reply belongs to another frozen package; never splice its state or results here. This cohort uses one private, otherwise empty project. The first question copies the owner-only frozen Greenwich A/B actor (private evidence folder) unchanged (SHA-256 `125e7cd0140f610b7db2454f33e0329cd2ccdcc1e9085dcd3e87c5bbe2469208`); advert extracts are user supplied and **do not authorize fetching their Foxtons links**. Preserve a byte-identical file for each follow-up and publish each hash in the private receipt.

Use five follow-ups and a real reopen. All inputs below are **user messages in the test**, not instructions from a source page. No additional tool/research permission is implied. The CLI headless `ask_user_question` may have no human present; if so mark native choice unavailable instead of fabricating a selection. For a real interactive native choice, record its literal labels and submitted reply bytes. Avoid repeating the whole history in a follow-up: exercise the state, not transcript copying.

| Human turn | Exact test message after first frozen input | Primary change/gate |
|---|---|---|
| 2 | `我想兩間都繼續比。A 我只是考慮可能去看，但我還沒有同意你聯絡或預約；B 也繼續研究。請查能否確定 B 的戶號與同戶 EPC、供暖和帳單，查不到就標未知。把目前 A/B 比較、來源和下一步正式存好。這輪依舊不要找新房或約看房。` | A considered only, B research continues. The first explicit save claim must be a **passing current comparison `save_gate` receipt**. |
| 3 | `房租上限改成每月 £2,200，只算房租，不代表房租加帳單的總花費。臥室晚上安靜從加分改成必要條件；沒有同戶夜間證據就保留待確認，不要用戶外分貝推成室內。A 即使超過新的上限也先留在比較裡，先不安排看房；B 繼續查證，同樣別聯絡或預約。通勤 45 分鐘左右仍可談。` | Change budget and quietness strength. A at £2,450 **exceeds global ceiling**, B £2,100 within; quietness unknown for both. Prior saved receipt must turn stale. |
| 4 | `我接受 A 這一間 £2,450／月的房租例外，用於繼續比較；但 A 暫時不看房。我的一般房租上限仍是 £2,200，B 和其他房源不得沿用 A 的例外。安靜還是必要條件，沒有同戶證據就待確認。請更新兩間的比較與下一步。` | A candidate-only exception and continued viewing hold. No global budget increase, no booking authority. |
| 5 | `請把現在這份 A/B 比較、每條條件的我的原話、來源收據和下一步正式存好，方便重開後接續；如果哪一部分做不到就直接說明。不要聯絡、預約，也不要抓新房源。` | Explicit current save again; current revision, evidence/review/context hashes and TODO must bind. |
| 6, new session | `我回來了。沿用上次保存的條件和研究，現在 A/B 哪些還能繼續研究，哪些可看房、哪些還不能？請說明為何，以及已核實與未知的公開資料。若需我決定一個取捨，問一個問題；不要重新抓廣告或聯絡仲介。` | Fresh Grok conversation, same private project/pinned ZIP, no replayed chat. Recover budget £2,200, A's exception/hold, quiet must unknown, B EPC/unit/bills unknown from state, not from this reminder. |

**Hard acceptance:** each human turn has a genuine terminal `end_turn` reply with exact actor and skill bytes, raw stream and official tool/source statuses, no unrequested listing fetch/contact/book; output never strengthens `consider` to `ready/booked/rejected`. Show A/B advert prices/beds/areas as reported and A EPC 81 m² separately; preserve B unknown unit EPC, bills and total monthly spend, Sketch `recently let` over stale cached button. A 9:00 arrival is mandatory, ~45 minutes negotiable; compare B's rail and bus routes under the same origin/date/destination/door assumptions rather than reject on only the 08:58 option. Outdoor Defra noise does not prove bedroom silence; B 56.7–63.4 vs road Lden 53 is 3.7–10.4 dB difference, not 7–12. Do not turn a 100 m mapped-night-economy zero into 300 m or none in reality. At turns 2 and 5 verify current private journal event hash and revision, evidence SHA, bound comparison/review/context, saved TODO and `save_gate --scope comparison` before accepting a model claim of saving. Turn 3 must invalidate the older receipt. At turn 6 verify the journal and source hashes independently **before reading the model reply**; compare reply and TODO to state. A full report requires a separate report gate if claimed. An attempted checker with zero findings is not a complete semantic/source verifier; independent reviewer must inspect first visible message, numbers, candidate authority and user tradeoff. Missing native option, source, tool or usage evidence = *unverified*, never pass.

**Experience gate, separate from correctness:** first visible message must say a real A/B difference or which relevant source is being checked and why; final should answer the question, offer research-based comparison and one useful next choice (no more than three essential questions), keep internal labels out of the user-facing answer and avoid repeating timestamps and arithmetic. Save the user's synthetic reactions/answers as exact turn text for future eval. Capture per-turn models/tools/input/cache/output/reasoning and provider-estimated USD without treating them as subscription charges; if the provider's weekly pool percentage is unavailable, record why and leave `$20/$30` capacity unquantified. Stop a hard/deadline breach without discarding its raw evidence or auto retry.

An honest full pass requires every gate above, **including genuine re-open**. One completed opening answer does not pass the journey. Grok Bot native choices/attachment enforcement and Grok Build CLI are distinct hosts; report them separately. No Claude model calls are part of this plan.
