# Conditional third pinned Grok source-and-authority repeat

Recorded after **both** original preregistered independent openings, but before a third provider call. Runs 01 and 02 completed and fixed the original B railway 08:36 versus Underground 08:58 endpoint confusion. Independent source/journal review found a distinct combined-gate failure in run 02: the model's journal records the exact user quote requiring arrival **before 09:00**, but the eligibility constraints, candidate ranking and TODO omit the hard arrival condition. Its final reply mentions arrival, which does not repair missing deterministic enforcement. The statement that *all* retrieved plans had no wait is too strong: bus/all-mode plans include 1–3 minutes of waiting. Both replies are still long and their first visible progress sentence describes internal procedure. Neither run proves a new upstream TfL fetch; several official receipts reused an earlier cached timestamp. These failures and the first two complete raw traces remain in the report, whether this repeat is favorable or not.

The original [paired preregistration](paired-source-check-plan-2026-09-16.md) calls for a third frozen independent repeat when one opening fails source or authority. Use **the same** public ZIP SHA-256 `1263662eea308d36428f770df76aad9dcd95d08e8678a9465b1a2225124563a9`, installed `SKILL.md` SHA-256 `a59b36f5fbc826ff088b24d2d32a5f1a361a11fcd5777c94fa4b8dc4aceb58ab` and opening actor SHA-256 `125e7cd0140f610b7db2454f33e0329cd2ccdcc1e9085dcd3e87c5bbe2469208`, untouched CLI 1.0.30, `--model grok-4.6` (the final provider usage reports backend `grok-4.6-build`), high effort, no subagents, fresh empty (branch worktree) project and isolated OAuth home. Same soft 14/hard 32/max 3,600 seconds/no auto retry. Commit this extension and seal third project's installed-file/actor/plan checksums in an owner-only manifest before dispatch; do not inherit either earlier research/state/session or change public source.

Check genuine terminal end, complete all/rail/bus TfL receipt statuses/endpoints/buffers/cache dates and the actual `commute_compare.py` tool call. Independently inspect **journal plus constraints, result, ranking and next actions** to establish whether the mandatory 09:00 arrival and exact user's quoted authority survive into deterministic checks; a correct sentence alone is insufficient. Check that an unknown same-unit EPC stays unknown, and that outdoor noise does not become required quietness. Detect any forbidden listing fetch/contact/booking. Audit first visible sentence and final reply separately. Report counts, processed fresh/cache/output and API-equivalent ticks for all three without treating them as paid-subscription usage. A good third answer cannot retroactively make the failed second pass; it can only characterize the variability and root cause for the next architecture fix.
