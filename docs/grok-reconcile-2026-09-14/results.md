# Grok Bot current-skill integration: status and evidence

Current status (2026-09-15): Grok shows a private native `pea-princess` loader, and the reconciled ZIP and full `SKILL.md` hashes were independently recomputed in the isolated Bot's remote computer. The isolated synthetic episode completed six rental turns and one export. Downloaded archive bytes, the isolated project's original objects and revision-29 journal/checkpoint support saving the comparison and TODOs at the fourth scripted turn's B/save followup. Final revision-37 working copies reflect the changed conditions, but those new copies have not been registered/revalidated after the old saved task was invalidated. Independent conversational UX/consent/research gates also failed; **this is a recorded compatibility test, not an accepted release**. See [the dated native validation report](native-observations-2026-09-15.md) for exact outcomes, priority and limits. The setup failure and UI-controller observations below are earlier 2026-09-14 history, not current installation status.

## Which version to use

| Part | Decision and evidence |
|---|---|
| Main625f95a (Claude work) | Integration base. Preserve brief EPC/roads/price commands, their callers and plan scaffold, plus latest conversation/pre-tool wording. |
| Boundary V4b376849 (Codex work) | Selectively port candidate-specific consent, explicit retention/release, atomic changes and current comparison/TODO artifacts. Previous18 Terra turns support this bounded state behavior, but all6 initial openers failed UX. |
| Original264-job condition-ledger experiment | Keep its do-not-adopt conclusion. Do not copy broad profile/schema/panel/report changes or automatic downgrading of ordinary mandatory checks. |
| Combined package | New test version; neither earlier result establishes Grok quality or savings. No main deployment or entire-branch merge. |

Main's brief-output study reports a39% reduction on Sonnet-standard cell means with9/10 pairs lower, and quality differences within observed baseline variation; Grok and other cells do not inherit that figure. Main625f95a's new first-response wording is motivated by reviewed complaints, not a completed measurement of this combination. The two efforts address different problems; there is no supported overall winner.

The isolated branch is `codex/grok-reconciled-skill-2026-09-14`. Selective commits:c6cb69e,350e6a9,a388fc8,817baa5. Subsequent aabb151 completes the detached reference fixture;91cf2ed shortens redundant router labels to meet the existing prompt limit. All original route destinations, essential rules and18 fixed questions are preserved. Main brief tools, profile/panel/report/viewer/onboarding and the existing live adapter remain unchanged by this integration. These consumers are not newly connected to the boundary schema.

## Validation and frozen package

Final source:91cf2ed9c10b07c999513468b621632bd0475340. Public identifier and ZIP root:`pea-princess`.

- 271 focused tests passed before full integration checks, including59 boundary tests and46 eligibility tests.
- Independent code review found no actionable issue; probes confirmed bare mandatory conditions still block and invalid retained provenance is rejected. Boundary logic matches V4; supporting state/eligibility ports preserve main's contract.
- The first full run had87 errors and1 failure across2925 tests:84 tests lacked generated distribution instructions in the new checkout,3 exceeded the8000-character prompt limit, and1 detached-reference fixture omitted the new boundary links. All are retained in the first log. These were offline failures, not Grok/model retries.
- After the focused fixes and build, the full suite passed:2925 run,2907 passed,18 skipped,136.955 seconds.77 targeted documentation/package tests also passed.
- Build:7962/8000 prompt characters;109 ZIP files,749979 bytes. ZIP SHA-256:`6c2151eba49f70a96d76308d45dbe99cb0f4d3384c1718dcc0f61a202fd1be55`. Every file matches the selected local source, including the shared viewer outside the skill source directory. SKILL.md SHA-256:`29a37756aaea0607f164b090348d0f98ed6abf1f742ff60f3b7f07b26e5690e2`.

An initial operator hash-check helper assumed the viewer lived inside the skill source directory and failed before writing a package receipt; correcting the mapping to the builder's actual shared viewer path resolved it. This was not a package mismatch. Source/package manifests and both full-suite logs are private.

## Historical Grok setup preflight (2026-09-14)

Installed native app then reported0.47.0. The old relevant tests showed `vet-flat` attachments and model-reported old hashes; no installed bytes were independently retrieved. Marketplace→Yours then showed no installed plugins and no private skills. Old loading was therefore not evidence of a native registered skill. Later installation of `pea-princess` is recorded in the dated checkpoint.

A fresh Bot was created and renamed **Pea Princess2026-09-14** (the UI includes spaces: **Pea Princess 2026-09-14**). Existing chats, pending questions and the old `KＣ` draft were preserved. Creating the Bot generated its own greeting/choice question before any setup; metadata edits also showed transient working status. The number of underlying model operations for these automatic events is unknown, and they are excluded from rental-quality samples.

Input preflight: one fast batched bot-name entry initially dropped its first letter; native setValue plus a separate blur corrected the name and the sidebar confirmed it. Separately focusing the composer before typing `INPUT-CHECK-0914` produced exact AX and screenshot text. It was cleared without sending. No long paste or Return-key submission was attempted.

The official [native skills guide](https://docs.x.ai/grok-bot/skills-routines-and-automations) describes private saved skills, per-Bot enablement and slash-menu references; [settings](https://docs.x.ai/grok-bot/settings-and-notifications) points to Marketplace/Yours. On this actual UI, Yours is visible but has no import button and instructs asking the Bot to create a private skill. Build CLI filesystem discovery is a different product; no `.grok` path is presumed to register a native Bot skill. Native setup will therefore provide the exact ZIP and ask for supported private registration, with attachment-only fallback explicitly distinguished if native registration is unavailable.

## Historical attachment/transport attempts and evidence limits

### Continuation after the user requested operator-only attachment

The exact 428-character local-setup trigger was verified in the composer, screenshot and sent-message AX at23:06:07 London time. It names only the three prepared input files, overrides the cloud location with an isolated local `native-workspace`, and prohibits unrelated file access, permission changes and ZIP/base64 dumps. The actor source and rental fixtures were not changed. The [official local-access guide](https://docs.x.ai/grok-bot/approvals-security-and-privacy) documents per-operation Allow once; no Always allow or general setting change was made.

The UI acknowledged one-time permission for `setup.txt` and `package-manifest.json`; Grok reported reading both. At23:07:50 it reported that later shell/copy/read access was denied, and at23:08:03 explicitly said the ZIP was unavailable, the requested workspace was unwritten, the skill was unregistered and the actual SKILL.md could not be delivered. A third one-time ZIP permission acknowledgment appeared later, followed by the host status “stopped, no reply”; this does not prove ZIP bytes were read or setup resumed. There is no retained underlying shell result establishing why access failed, and no basis to attribute denial to the user, timeout, controller or another mechanism conclusively.

A native `setup-receipt.json` artifact card (2.4kB) appeared. Save opened the native save dialog, whose Save button stayed disabled after a unique filename and focus change. The dialog was cancelled; the intended Downloads file does not exist, and the isolated workspace is empty. Thus artifact-card existence is observed, but its complete bytes, tool errors and hashes remain unverified. Root retained the exact UI observations privately. Coordinate operations also returned `noWindowsAvailable` in Finder, which supports a controller-wide symptom rather than a proven Grok-specific file defect. Some AX and screenshot observations represented different visible stages; neither should be silently substituted for the other as a complete trace.

Separately, an initial reminder choice was observed answered with followups timestamped22:27:29/31, between the previous pause and this continuation. Independent audit found no recorded CUA action at that time. Its source is unknown; retain it as an extra work-triggering interaction, not a rental sample. Current counters: one intended setup text, one uncertain reminder choice, three observed one-time permission acknowledgments, zero rental turns; physical model calls and tokens are unknown. The original eight combined-interaction bound and the prospective local-transport amendment are both retained in [the plan](plan.md). Permission handling is not treated as free usage.

At that historical point, AppleScript was not yet authorized. The user later authorized it explicitly; the later three completed attachments cannot be attributed to the immediately preceding AppleScript action, so no success claim is based on that action. Later package integrity and native registration are covered in the dated checkpoint.

### Earlier attachment attempts (retained history)

The unlock reply was captured and work resumed against the unchanged package. Native AX confirmed the correct ZIP path, but the file chooser's Open button stayed disabled. The same occurred with plain-text setup selected and with byte-identical files copied to a dedicated visible Desktop folder, so neither ZIP format nor a hidden source directory alone explains the observations. Coordinate clicks returned `noWindowsAvailable`; some element actions returned `elementHasNoFrame` or made no visible change, while keyboard selection and cancellation worked. Resetting the UI session and requesting an app restart did not yield a verified attachment. The post-restart UI briefly showed a stored-message/connection banner and then restored the original greeting. No attachment, deliberate setup message or rental turn was verified as submitted. These are UI-transport observations, not proof of a Grok upload/product defect or a failed skill/model test.

The earlier fallback request asked for manual attachment of only `pea-princess-current.zip`, `package-manifest.json` and `setup.txt` from `pea-princess-grok-20260914-setup` on the Desktop. Those files were byte-identical to frozen materials and future turns were absent. Later completed attachments and independent remote hash recomputation supersede this setup blocker; their sender remains unattributed. Existing chats and the old draft remain visible; broad permissions and the tested actor package have not been changed.

The [frozen plan](plan.md) originally anticipated at most eight combined deliberate submissions (setup, six rental turns and export); its pre-rental local-transport amendment bounded eight **intended text** submissions plus narrowly scoped native decisions and retained the original combined count. The completed ledger distinguishes those eight texts, three clicked fixed choices, one file-only setup, two unattributed original-Bot choices and one earlier unrelated uncertain choice. Only the current turn's synthetic material was supplied; future turns and grading expectations were withheld. Cases cover candidate-only 46-minute consent, an update to 75, a heating-fee interruption, retention without viewing, a global change that must not erase retention, and explicit release. No live listings, scan performance, token savings, stochastic reliability or real user satisfaction claim follows from this single synthetic episode.

The final six-turn independent Astra review and root evidence audit are complete in the [dated native report](native-observations-2026-09-15.md). The scoped export archive was actually downloaded and rehashed against remote bytes; historic revision-29 saving passes while current revision-37 durable output revalidation fails. Native model, effort, token counts, per-turn resource reads and complete hidden tool execution remain unknown. Raw AX may truncate long text independently of visible text; screenshots, client snapshots and downloaded artifacts support only what they expose.

An independent Astra [evaluation preflight](evaluation-preflight.md) was completed before deliberate submissions. It identifies setup priming, clarifies the relationship between the semantic integration and final package commits, and records how turn 4's ambiguous wording will be judged: retention and no booking are required, while merely considering a viewing is not automatically a failure. Either A/B ranking may pass when its tradeoffs and evidence limits are explained. This prevents grading the actor against unexpressed intent. No actor fixture or expected outcome was changed after observing a response; at preflight there were no rental responses. Setup behavior and evidence limits receive their own independent review, without a rental-quality score.
