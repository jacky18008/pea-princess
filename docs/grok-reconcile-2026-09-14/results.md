# Grok Bot current-skill integration: status and evidence

Status: the reconciled test package is ready, but native Grok setup and quality testing are **paused before the first deliberate submission**. The user unlocked macOS; the subsequent native attachment workflow could not be completed through the UI controller. Manual attachment of the three prepared setup files has been requested. There is no valid current-package Grok quality result yet. Do not interpret the host's automatic greeting or a renamed Bot as skill installation.

## Which version to use

| Part | Decision and evidence |
|---|---|
| Main625f95a (Claude work) | Integration base. Preserve brief EPC/roads/price commands, their callers and plan scaffold, plus latest conversation/pre-tool wording. |
| Boundary V4b376849 (Codex work) | Selectively port candidate-specific consent, explicit retention/release, atomic changes and current comparison/TODO artifacts. Previous18 Terra turns support this bounded state behavior, but all6 initial openers failed UX. |
| Original264-job condition-ledger experiment | Keep its do-not-adopt conclusion. Do not copy broad profile/schema/panel/report changes or automatic downgrading of ordinary mandatory checks. |
| Combined package | New test version; neither earlier result establishes Grok quality or savings. No main deployment or entire-branch merge. |

Main's brief-output study reports a39% reduction on Sonnet-standard cell means with9/10 pairs lower, and quality differences within observed baseline variation; Grok and other cells do not inherit that figure. Main625f95a's new first-response wording is motivated by reviewed complaints, not a completed measurement of this combination. The two efforts address different problems; there is no supported overall winner.

The isolated branch is `codex/grok-reconciled-skill-2026-09-14`. Selective commits:c6cb69e,350e6a9,a388fc8,817baa5. Subsequent aabb151 completes the detached reference fixture;91cf2ed shortens redundant router labels to meet the existing prompt limit. All original route destinations, essential rules and18 fixed questions are preserved. Main brief tools, profile/panel/report/viewer/onboarding and the existing live adapter remain unchanged by this integration. These consumers are not newly connected to the boundary schema.

## Validation and frozen package

Final source:91cf2ed9c10b07c999513468b621632bd0475340. Public identifier and ZIP root:`pea-princess`.

- 271 focused tests passed before full integration checks, including59 boundary tests and46 eligibility tests.
- Independent code review found no actionable issue; probes confirmed bare mandatory conditions still block and invalid retained provenance is rejected. Boundary logic matches V4; supporting state/eligibility ports preserve main's contract.
- The first full run had87 errors and1 failure across2925 tests:84 tests lacked generated distribution instructions in the new checkout,3 exceeded the8000-character prompt limit, and1 detached-reference fixture omitted the new boundary links. All are retained in the first log. These were offline failures, not Grok/model retries.
- After the focused fixes and build, the full suite passed:2925 run,2907 passed,18 skipped,136.955 seconds.77 targeted documentation/package tests also passed.
- Build:7962/8000 prompt characters;109 ZIP files,749979 bytes. ZIP SHA-256:`6c2151eba49f70a96d76308d45dbe99cb0f4d3384c1718dcc0f61a202fd1be55`. Every file matches the selected local source, including the shared viewer outside the skill source directory. SKILL.md SHA-256:`29a37756aaea0607f164b090348d0f98ed6abf1f742ff60f3b7f07b26e5690e2`.

An initial operator hash-check helper assumed the viewer lived inside the skill source directory and failed before writing a package receipt; correcting the mapping to the builder's actual shared viewer path resolved it. This was not a package mismatch. Source/package manifests and both full-suite logs are private.

## What was actually observed in Grok

Installed native app reports0.47.0. The old relevant tests show `vet-flat` attachments and model-reported old hashes; no installed bytes were independently retrieved. Marketplace→Yours now explicitly shows no installed plugins and no private skills. Old loading was therefore not evidence of a native registered skill.

A fresh Bot was created and renamed **Pea Princess2026-09-14** (the UI includes spaces: **Pea Princess 2026-09-14**). Existing chats, pending questions and the old `KＣ` draft were preserved. Creating the Bot generated its own greeting/choice question before any setup; metadata edits also showed transient working status. The number of underlying model operations for these automatic events is unknown, and they are excluded from rental-quality samples.

Input preflight: one fast batched bot-name entry initially dropped its first letter; native setValue plus a separate blur corrected the name and the sidebar confirmed it. Separately focusing the composer before typing `INPUT-CHECK-0914` produced exact AX and screenshot text. It was cleared without sending. No long paste or Return-key submission was attempted.

The official [native skills guide](https://docs.x.ai/grok-bot/skills-routines-and-automations) describes private saved skills, per-Bot enablement and slash-menu references; [settings](https://docs.x.ai/grok-bot/settings-and-notifications) points to Marketplace/Yours. On this actual UI, Yours is visible but has no import button and instructs asking the Bot to create a private skill. Build CLI filesystem discovery is a different product; no `.grok` path is presumed to register a native Bot skill. Native setup will therefore provide the exact ZIP and ask for supported private registration, with attachment-only fallback explicitly distinguished if native registration is unavailable.

## Remaining steps and evidence limits

The unlock reply was captured and work resumed against the unchanged package. Native AX confirmed the correct ZIP path, but the file chooser's Open button stayed disabled. The same occurred with plain-text setup selected and with byte-identical files copied to a dedicated visible Desktop folder, so neither ZIP format nor a hidden source directory alone explains the observations. Coordinate clicks returned `noWindowsAvailable`; some element actions returned `elementHasNoFrame` or made no visible change, while keyboard selection and cancellation worked. Resetting the UI session and requesting an app restart did not yield a verified attachment. The post-restart UI briefly showed a stored-message/connection banner and then restored the original greeting. No attachment, deliberate setup message or rental turn was verified as submitted. These are UI-transport observations, not proof of a Grok upload/product defect or a failed skill/model test.

The user has been asked to bring the named Bot forward and manually attach only `pea-princess-current.zip`, `package-manifest.json` and `setup.txt` from `pea-princess-grok-20260914-setup` on their Desktop, without sending. Its three files are byte-identical to the frozen materials; future turns are absent. Then verify displayed filenames and the exact short setup trigger before sending once. Inspect actual setup response, native skill name/enabled state and downloadable bytes. A claimed checksum alone is not independent byte verification. Existing chats and the old draft remain visible; broad permissions and the tested package have not been changed.

The [frozen plan](plan.md) allows at most8 deliberate submissions:setup,6 rental turns and export. Only the current turn's attachment is supplied; future turns and grading expectations remain withheld. Cases cover candidate-only46-minute consent, an update to75, a heating-fee interruption, retention without viewing, a global change that must not erase retention, and explicit release. No live listings, scan performance, token savings, stochastic reliability or real user satisfaction claim follows from this single synthetic episode.

A fresh independent Astra conversation review and root review remain pending because no quality conversation exists. Precise native model/effort/token counts, hidden tools and file export remain unknown unless actually exposed. Raw AX may truncate long text independently of visible text; screenshots and native tool observations support only what they expose. Private raw observations are retained from this Codex task's UI tool records, not hidden Grok app data.

An independent Astra [evaluation preflight](evaluation-preflight.md) was completed before deliberate submissions. It identifies setup priming, clarifies the relationship between the semantic integration and final package commits, and records how turn 4's ambiguous wording will be judged: retention and no booking are required, while merely considering a viewing is not automatically a failure. Either A/B ranking may pass when its tradeoffs and evidence limits are explained. This prevents grading the actor against unexpressed intent. No actor fixture or expected outcome was changed after observing a response; there are still no rental responses.
