# Handoff — 2026-09-11 evening (Claude session, before compaction)

Read this first in the next session. Private corpus text stays out of this file; results folders under
`bench/private/` are not committed. Repository HEAD at hand-off: see `git log --oneline -60` (about 50
commits today, none pushed; the author pushes).

## Decisions the author made today (do not re-open)

1. **Precedence**: "The person's instruction outranks every default in this skill, in every host" is the
   first line of `SKILL.md` and repeated in `references/rules.md`. Four things do not move: no invented
   numbers; no ethnicity or nationality as a factor; the scripts read only open registers and the skill
   never suggests fetching a listing site; untrusted inputs never authorize anything.
2. **Listing links**: the skill does not open them and never suggests it; it says so once and asks for
   the page (PDF, screenshots, text). The two link-specific authority sentences were removed after the
   general-only experiment (Opus/Haiku still follow the person 3/3 on push-back; Sonnet holds 0/3).
   The "launch 前必回頭" item is closed.
3. **Scope**: finding listings is outside the skill — neither restricted nor managed. The skill narrows
   the map (area sweep → one area, the portal search recipe, a watch list of 3–5 buildings), the person
   searches the portals and sets alerts, pastes what they like, the skill vets.
4. **Seed sharing**: one card only, never a commute district or move-in month; story summary only with
   `--with-story` and scrubbed; free text scrubbed of postcodes, boroughs, "X station", "X Road";
   encouragement line + rule at the moment of posting ("share your taste and tips, keep your whereabouts").
5. **Pages are views, not forms**: `scripts/panel.py` renders profile.yaml to a read-only page; no
   interactive requirements form. Report page unchanged (`render.py` / `viewer/viewer.html`).
6. **Deprivation data**: only the Living Environment domain (`scripts/living_env.py`); income never.
7. **Playground sandbox**: live research runs `workspace-write` with network (was read-only → scripts
   could not fetch → 815k-token web-search turn).
8. **Installed copies refreshed** from the repo: `~/.agents/skills/pea-princess` (Codex) and
   `~/.claude/skills/pea-princess` (Claude Code); the Aug-31 private `vet-flat` moved to
   `~/.claude/skills-archive/vet-flat-2026-08-31`.

## What was built today (all committed)

- `scripts/listing_fields.py` (saved page → fields, refuses URLs), `scripts/living_env.py`,
  `scripts/panel.py`, `scripts/reply_check.py` (pre-send: numbers without a home, jargon, simplified
  characters, >3 questions, question after a go-ahead, praise opener), `scripts/area_scan.py` (one call
  per street: crime, works incl. notable recent, roads/rail/night economy/parks, living environment,
  plain reading; quiet ≠ safety; centroid caveat), `planning.py --brief`, `extensions/` (personal
  add-ons), `references/rules.md`, `references/axes/README.md`, `references/requirements-contract.md`,
  `references/listing-fields.md`, seed privacy rules, `docs/research/*2026-09-11.md`,
  `docs/EXPERIMENTS.md` sections (link-paste probe, insistence probe, intake probe, token A/B).
- Bench: `bench/link_probe.py` (link paste, push-back, `--skill-dir`), `bench/intake_probe.py`,
  `bench/history_replay.py` (private-corpus replay; judge v1 + v2; `--rejudge`, `--calibrate`,
  `--retry-failed`; merged view keeps the latest good row per key), `bench/replay_review.py`
  (checkpoint review by Opus → harness proposals).

## Experiments: state at hand-off

Results folder: `bench/private/history-replay-2026-09-11/` (`rows.jsonl`, `judge_v2.jsonl`,
`reviews/`, `calibration/`, per-run logs). Report: `python3 bench/history_replay.py --report <folder>`.

| Run | State | What to do |
|---|---|---|
| Baseline Claude chain (Sonnet std/lite, Opus, Haiku; ask + reaction) | ask turns done; reaction turns running (`claude.log`, ends with CLAUDE-DONE) | when done: `--rejudge` |
| Baseline Codex chain (terra std, sol std, terra lite) | running (`codex.log`, CODEX-DONE); a waiter then runs `--retry-failed --agent codex` | when done: `--rejudge` |
| Outage retries (Claude) | running (`retry-claude.log`, RETRY-CLAUDE-DONE); Haiku 12 and Opus 3 rows were "API Error" during a DNS outage | when done: `--rejudge`; failed rows are superseded automatically |
| variant-a (checkpoint rules) vs main, Sonnet std ask | done, v2-judged | result in the table below |
| variant-b (variant-a + "open with the answer") and main-2 (baseline repeat) | variant-b done (15), main-2 at 5/15 (`variant-b.log`, VARIANT-B-DONE); a waiter then rejudges and prints | compare main / main-2 / variant-a / variant-b on v2; main-2 shows run-to-run noise |
| Judge calibration (judge vs real reactions on the original answers) | done: 9/14 exact, judge compresses to "1"; with the person-profile prior: 9/14, no better | keep judge v2 without the profile; treat "satisfy" as coarse |
| Token A/B (street question, Codex) | done, recorded in EXPERIMENTS.md: one scan per street → input tokens 20–60× lower, wall 3–7× lower | blind quality: old won 3/3 (see below) |
| Defra noise endpoints research agent | running (writes `scratchpad/research-defra-noise.md`) | read it before writing `scripts/noise.py` |

Variant skills live in the session scratchpad (`skill-variant-a`, `skill-variant-b`, `skill-before`,
`skill-general-only`); they are copies of `skills/vet-flat` with the edits described in
`reviews/claude-sonnet-5-standard-ask-main.md` (the checkpoint review) and `bench/replay_review.py`.
The scratchpad is session-local: if it is gone, rebuild variant-b from the review note (six-rule
"Before sending" section in `references/conversation-quality.md`, a "Before sending" default in
`rules.md`, the Output line in `SKILL.md`, axis 04 `--brief`, plus rule 0 "Open with the answer").

First ablation (Sonnet, standard, ask turn, 15 real cases, judge v2): main → variant-a: G1 facts pass
2→5, questions/reply 1.2→0.5, plain language T2 1.5→1.7, numbers without cue 3.3→2.9; but first
sentence pass 9→6 (praise openers), task 1.00→0.87, satisfy 0.80→0.67, beats original 2→0. Hence
variant-b. Codex terra on the same turn: T mean 1.86, first sentence 10/12 — the strongest of the
cheap models on this replay.

Token A/B quality (Opus blind, 3 pairs): the expensive old run won 3/3 because it (1) reverse-geocoded
the input and noticed the centroid was off the street, (2) web-searched the Defra noise map (decimal
dB values it could not source), (3) read all 343 planning records and found a demolition/construction
application at 134 m. Fixes 3 and 4 (notable recent works; quiet ≠ safety) are in `area_scan.py`;
fixes 1 and 2 (street sampling via Overpass way geometry; `scripts/noise.py` from the Defra WFS —
Round 3 works: `road-noise-lden-england-round-3`, typeName
`dataset-fd1c6327-ad77-42ae-a761-7c6a0866523d:Road_Noise_Lden_England_Round_3`, bbox in lon,lat order,
`noiseclass` bands ≥55 dB; lnight `dataset-cc48e728-602a-4e8a-9221-49f661ab58f8:Road_Noise_Lnight_England_Round_3`)
are the next build. **Noise endpoints are now verified** (`docs/research/defra-noise-endpoints-2026-09-11.md`):
Round 4 (2022 data) WMS `GetFeatureInfo` returns an actual dB value at a point (`GRAY_INDEX`; road slug
`road-noise-all-metrics-england-round-4`, rail slug `noise-data`; layers `Road_Noise_Lden_England_Round_4_All`,
`..._Lnight_...`, rail equivalents; King Street 75.29 dB Lden, Clapham Junction rail 72.22) — this is what the
expensive run had found by hand; Round 3 bands via OGC API Features `/spatialdata/<slug>/ogc/features/v1/collections/<Layer>/items?bbox=lon,lat,lon,lat`.
Licence OGL v3, attribution "© Crown Copyright". `scripts/noise.py` should return road/rail Lden and Lnight
values (one decimal) plus the Round 3 band, with the caveats in that memo (pixel alignment, 2021 traffic).
The author agreed to tier the scan by depth (lite: one point; standard: three
points along the street + stages for the top notable; deep: + 500 m, rail and night layers) — the
summary size stays the same, only requests and time differ.

## Next steps, in order

1. When the waiters report: `python3 bench/history_replay.py --rejudge bench/private/history-replay-2026-09-11`
   then `--report`; write the ablation (main, main-2, variant-a, variant-b) and the per-model table into
   `docs/EXPERIMENTS.md` (mark outage-affected rows; n=15 single runs; judge compresses satisfy).
2. If variant-b beats main and main-2 on gates/first sentence/task without losing satisfy: move its
   text into the repo (`conversation-quality.md` checkpoint, `rules.md` default, `SKILL.md` Output line);
   keep the prompt pack ≤ 8,000 chars (`python3 tools/build_dist.py`).
3. Build `scripts/noise.py` (Defra WFS, bands, point-in-polygon, fixture test, sources.yaml entry) and
   street sampling + depth tiers in `area_scan.py`; re-run the token A/B (3 pairs) and the blind quality
   comparison; record.
4. Run the checkpoint review (`bench/replay_review.py`) for Opus and Codex terra configurations and
   fold their harness proposals into the next variant.
5. Persona playground: re-run the Foxtons conversation with network on and the new skill; compare the
   per-turn tokens with the 815k baseline (the other session owns `tools/persona_playground.py`).

## Pending decisions for the author

- ONS London private-rent statistics for the area/typical-rent step (recommended).
- Open-register fetchers: switch `BROWSER_UA` → honest `TOOL_UA` (recommended; a few council portals may block).
- Codex "said it did / did it" honesty check as a default line (deferred).

## Caveats to carry forward

- A DNS outage on the hotspot invalidated ~30 rows (API Error replies); retries supersede them, but
  any table printed before the retries finished is contaminated for Haiku, Opus and Codex sol.
- Codex reads a skill by name from `~/.agents/skills/` before the workdir copy; keep the installed copy
  current (`rsync -a --delete skills/vet-flat/ ~/.agents/skills/pea-princess/`).
- The judge's "satisfy" is coarse (predicts 1 for praise and complaints alike); rely on gates, T and
  vs-original; programmatic counts (numbers without cue, questions) are the steadiest signals.
- All counts are single runs per cell; direction, not rates.

## Context slimming — plan only, nothing started (author: design the experiment first, version everything)

Where the tokens go: every tool call re-sends the whole context (system prompt, skill text, conversation,
every earlier tool result); standard depth adds 15–25 calls per flat, and each big tool result is re-sent
on every later call (one 80k-character planning dump was re-sent ~30 times ≈ 0.8M tokens). Caching is
already good (Claude 88–92%, Codex 87–97%), so the levers are calls and result size, not the prefix.

What the vendors say (from their documentation, from memory; verify the pages before quoting):
Anthropic prompt caching (stable prefix first, volatile last); Anthropic context engineering for agents
(smallest high-signal set; just-in-time retrieval; structured notes in files so the conversation can be
compacted; sub-agents that return summaries); Anthropic context editing (clear used tool results);
Claude Code (sub-agent output stays out of the main context; short CLAUDE.md; compaction with
summaries); Agent Skills spec (SKILL.md < 500 lines, references on demand); OpenAI Responses caching
(instructions and examples first, user data last, stable order); Codex (short AGENTS.md with a byte cap,
reasoning effort, web-search context size low, sub-agents).

Three places with room, none of them the skill text:
1. **Tool results**: scripts return a fixed summary; the raw records stay in files (`area_scan.py` is the
   pattern). Candidates: planning (stages/officer report), EPC certificate, company profile/filings,
   reviews digest. Expected: standard per flat from ~1.5–2.5M toward ~0.3M ●.
2. **Old conversation**: the ledger file (`.pea-state`, profile) is written before compaction; the
   checkpoint rule becomes "ledger file first, conversation second".
3. **How the model reads files**: "read the one section the route names", not `sed -n '1,240p'` dumps.

Do not touch: the skill text (already small; cutting it cuts rules) and the host system prompt.
Sub-agents for isolated reading: theoretical gain, but the role-pipeline ablation (2026-09-07) found no
quality gain at 2–4× cost — only with a measured experiment.

### Experiment design (before any change ships)

- **Bench**: the existing full-vet benchmark (`bench/run.py`, `bench/grade.py`, private gold, the same
  5 flats) for quality and tokens, plus the private-corpus replay (15 cases, judge v2 pinned to Opus 5,
  no profile prior) for conversation quality, plus the programmatic checks (numbers without cue,
  questions, jargon).
- **Arms**: baseline (frozen tag) · A = per-axis summary scripts behind flags · B = A + ledger-first
  compaction rule · C = A + read-the-section instruction. Change one thing per arm.
- **Cells**: models Sonnet 5 and Codex terra · depths lite and standard · 2 repeats per cell (paired
  by flat and by case; report paired differences; n stays small — direction, not rates).
- **Metrics**: landmine recall/precision, fact recall, fabrications per run, verdict match, unknown
  axes; tokens per run split into fresh input, cache read, output; tool calls; wall time; replay v2
  gates, T dims, first sentence, satisfy, vs-original.
- **Gate to adopt**: no quality metric worse than the baseline's run-to-run noise (use main vs main-2
  as the noise estimate) and tokens at least 30% lower; otherwise the arm is recorded and dropped.
- **Version control**: tag the baseline (`git tag baseline-context-<date>`); each arm on its own branch
  (`exp/context-slim-a`, `-b`, `-c`) and, for replay runs, a `--skill-dir` snapshot made with
  `git archive` into `bench/ab/skill-variants/skill-<commit>`; every results folder records the commit
  of the skill it ran; scripts change behind flags so the baseline stays reproducible; the judge model
  and prompt are not changed during the experiment; merge only the winning arm, one commit per arm,
  with the numbers in the message and in `docs/EXPERIMENTS.md`.


## Night session (same day, after compaction) — state at 23:20

Background runs still going at the time of writing (all under `bench/private/history-replay-2026-09-11/`):
Opus reaction turns (`claude.log`, at case-10/15), the Codex chain's third run (`codex.log`; terra
reaction rows, one timed out at 72 tool calls), the rejudge of every unjudged row (`rejudge4.log`,
`judge_v2.jsonl` growing; a waiter then writes `report4.txt`), and the codex retry waiter (fires on
CODEX-DONE). The checkpoint reviews for Opus and Codex terra are done (`reviews/claude-opus-5-…`,
`reviews/gpt-5.6-terra-…`).

Done tonight:
- **Branch `exp/area-scan-tiers`** (tag `baseline-scan-2026-09-11` = 8da9223 is the baseline; commit
  cc6f1c2 the arm): `scripts/noise.py` (Defra Round 4 WMS point values in dB, Round 3 band; "not drawn"
  never 0 dB; WHO-anchored reading) and `area_scan.py` v2 (`--street NAME` looked for by name out to
  1.5 km, ways joined, the scan moves onto the street and says how far off the point was; road noise at
  three points ~120 m apart; `--depth lite|standard|deep`; the notable-works date bug fixed — Datahub
  dates are DD/MM/YYYY, so "since 2024" never matched before; householder noise filtered; an empty crime
  box re-run at 600 m). 28 new/updated tests; pack 7,994 chars. On the W6 centroid the scan now finds
  the 236 m offset and the 134 m demolition/dust-plan/hoarding site the expensive run had found by hand.
- **A/B running**: `bench/street_ab.py` (on main, b92537f) — before = skill-8da9223, after =
  skill-cc6f1c2 snapshots under `bench/ab/skill-variants/`, Codex terra, 3 pairs, each arm in a private
  HOME (Codex then lists only that HOME's skills — verified); results `bench/private/street-ab-2026-09-11/`
  (`run.log`, `rows.jsonl`, `raw/`, then `quality.json` from a blind Opus read, then `--report`). Adopt
  gate for the arm: the blind read finds centroid-off-street / noise dB with source / works-about-to-start
  more often in "after" with no loss on numbers-without-source, and input tokens not more than 2× before.
  If adopted: merge the branch into main (fast-forward), refresh the installed copies, record in EXPERIMENTS.md.
- **reply_check.py** (main, 9fc7f97): four checks from the two new reviews — machine paths, third-person
  "使用者", unexplained board codes/acronyms, empty "已驗證" claims. Inert until the checkpoint text ships.
- **history_replay.py `--inject-date`** (main): tells the answerer the date of the message it answers, from
  the case JSON sidecar (`case_date`); off by default so the running baseline chain is untouched. The next
  replay round runs every arm with it (main-3 as the control), so the date rule and the text are not confounded.
- **variant-c prepared** in the scratchpad (`skill-variant-c` = variant-b + rule 3 extended with named-point
  coverage and "a risk comes with its distance", rule 7 dates, rule 8 "a change lists what it changes",
  rule 9 "short in, short out", the new reply_check). Launch only after the variant-b verdict: if variant-b
  beats main/main-2, variant-c is the next arm (Sonnet std ask, 15 cases, `--inject-date`, against main-3).

Not touched: `tools/persona_playground.py` (other session), nothing pushed, no context-slimming change.

## State at 2026-09-12 00:05 (same night, continued)

**Branch**: everything tonight is on `exp/area-scan-tiers` (main merged in at bdf0087; the other session's
two commits c79e463 / 0245af5 also landed here because HEAD was on it). One arm = one commit, each with
its numbers; the baseline is tag `baseline-scan-2026-09-11`; skill snapshots per arm under
`bench/ab/skill-variants/skill-<commit>`. Merge to main = fast-forward when the arms below are settled.

**Adopted on the branch**
- Replay variant-b (1df20ce): the "Before sending" checkpoint + rules.md default + SKILL.md Output line.
  Numbers in the commit and in `docs/EXPERIMENTS.md` (replay section, f98b6da). main-2 was 7/15 judged
  when written; the table says so — refresh it from `--report` when `report4.txt`/`report5.txt` exist.
- Street scan v2 + noise.py (cc6f1c2, 01d6b14, de7332c, c2124f7): not yet merged to main in spirit —
  the arm's A/B decides. Codex blind read: after wins 2/3; Claude: after 3/3 but 5 of those 6 runs had no
  data (allow-list; re-run below). Codex tokens across all threads: before 0.63–2.30M, after 3.98–9.15M
  (sub-agents chose --depth deep and read the script source) → arms after-b (de7332c: the output's first
  key says "complete, do not read the source or cache") and after-c (c2124f7: "in this thread, never a
  sub-agent" in SKILL.md + rules.md) are queued to test whether the text tames that.

**Two measurement findings that change earlier numbers**
1. Codex (0.153, `multi_agent` on) spawns sub-agent threads to run the scripts; `codex exec --json`
   shows neither their commands nor their tokens. Rollout files under `~/.codex/sessions/` do
   (`session_meta.parent_thread_id`). `bench/street_ab.py --account` sums main + descendants. The
   earlier token A/B (EXPERIMENTS.md) is corrected; every Codex replay row has the same gap (thread id
   in its raw stream) — account before quoting a Codex token figure. `codex exec --disable multi_agent`
   exists but is not the user's default; a note for INSTALL.md is pending the after-b/after-c results.
2. The replay's Claude allow-list matched one spelling of the script call; other spellings were refused
   silently → some replay answers were written without data. `--claude-allow "Bash(python3 *),Bash(cd *)"`
   (opt-in, 8ec7072) fixes it; the deny hook still blocks curl/wget/URLs. Round 2 uses it.

**Running now (results folders)**
- `bench/private/street-ab-2026-09-11/` — done (Codex 3 pairs, Claude 3 pairs invalid); `quality.json`.
- `bench/private/street-ab-2026-09-11-claude/` — Claude Sonnet re-run with the allow-list, 3 pairs, then judge.
- `bench/private/street-ab-2026-09-11-b/` — Codex, arm after-b vs baseline, 3 pairs → account → judge → report.
- `bench/private/street-ab-2026-09-11-c/` — queued after -b: Codex, arm after-c vs baseline.
- `bench/private/history-replay-2026-09-11/` — the original session's rejudge still running (report4.txt
  when done); round 2 launched: `main-3` (skill-f98b6da = HEAD with variant-b) then `variant-c`
  (skill-f98b6da-c = + rule 3 extension, rules 7 dates / 8 what-this-changes / 9 short-in-short-out), both
  Sonnet std ask, `--inject-date`, `--claude-allow`; logs `round2-main3.log`, `round2-variant-c.log`
  (ROUND2-RUNS-DONE, then a rejudge → `rejudge5.log`, ROUND2-JUDGED, `report5.txt`).
- Baseline chains: Haiku reaction turns (`claude.log`), Codex terra reaction (`codex.log`), then the codex
  retry waiter.

**What to do with each result**
- Street arm: adopt if after-b or after-c keeps the quality wins (centroid-off-street, noise dB with source,
  works signal) and brings Codex all-thread tokens within 2× of baseline; otherwise adopt the scan change
  with `--depth lite` as the router default and record the cost. Then merge, refresh installed copies
  (`rsync -a --delete skills/vet-flat/ ~/.agents/skills/pea-princess/` and `~/.claude/skills/pea-princess/`),
  write the section in EXPERIMENTS.md.
- variant-c vs main-3: adopt on the same rule as variant-b (gates / first sentence / task not worse than
  noise, steady counts not worse); main-3 vs main/main-2 also shows what date injection + allow-list did.
- Then re-cut the context-slimming baseline tag on the merged main.

**00:45 addendum.** Arm after-b (self-describing output) did not tame Codex: pair 0 spawned five
sub-agents (135 calls, 10.7M tokens all threads), pair 2 answered "no data" because Codex's exec tool
returned empty after 30 s while the cold scan took 40–50 s. Root cause measured: `crime.box` ran five
shifted boxes × six months = 30 police requests (~36 s). Arm after-d (a5806ea, snapshot
`skill-a5806ea`) = after-c text + registers in parallel + centre-only crime box + a stderr line naming
the run and the saved JSON copy (`--out`/`--no-save`): cold wall 17.5 s. Queue on Codex now: after-c
(running) → nomulti (both arms `--disable multi_agent`) → after-d; results folders
`street-ab-2026-09-11-{c,nomulti,d}/`. Arm b is recorded in `street-ab-2026-09-11-b/` (after 2/3 on
the blind read; tokens 10.66M / 2.18M / 0.31M vs 0.54M / 2.48M / 0.90M). Adoption call for the whole
street arm: quality is settled (Claude 3/3 with fewer tokens; Codex 2/3 in every arm); the Codex cost
question is decided by nomulti vs after-d — if after-d brings all-thread tokens within 2× of baseline in
2 of 3 pairs, merge as is; otherwise merge with an INSTALL.md note recommending
`codex --disable multi_agent` (priced by the nomulti run) and record the numbers.
